---
title: Exemplo de arquivo .md
date: '2016-03-08'
language: pt
tags: ['markdown', 'código', 'funcionalidades']
authors: ['marlonPassos']
draft: false
summary: Exemplo de arquivo markdown com blocos de código e realce de sintaxe
---

Um exemplo de publicação com remark.

## Realce em linha

Exemplo de realce em linha `sum = parseInt(num1) + parseInt(num2)`

## Blocos de código

Código Javascript

```javascript
var num1, num2, sum
num1 = prompt('Digite o primeiro número')
num2 = prompt('Digite o segundo número')
sum = parseInt(num1) + parseInt(num2) // "+" significa "adicionar"
alert('Soma = ' + sum) // "+" significa combinar em uma string
```

Código python 🐍

```python
def fib():
    a, b = 0, 1
    while True:            # Primeira iteração:
        yield a            # yield 0 para começar e então
        a, b = b, a + b    # a agora será 1, e b também será 1, (0 + 1)

for index, fibonacci_number in zip(range(10), fib()):
     print('{i:3}: {f:3}'.format(i=index, f=fibonacci_number))
```
